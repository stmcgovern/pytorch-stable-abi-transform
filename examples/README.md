# Example Configurations

Sample `.stable-abi.yaml` configs for real projects. Edit the paths to match your environment, then run `stable-abi-transform` from the project root.

- **vllm/** — [vLLM](https://github.com/vllm-project/vllm) CUDA kernels
